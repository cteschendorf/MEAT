# Local development

Use Node.js 22.

```bash
npm install
npm run typecheck
npm run lint
npm test
npm start
```

Prefer Expo Go for early development whenever the current feature set is supported there.

Do not run `eas build` without explicit approval for that specific build.
