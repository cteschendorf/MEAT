# Optional public override for local Worker development. The USDA API key must
# remain only in the Cloudflare Worker secret named USDA_FDC_API_KEY.
EXPO_PUBLIC_USDA_PROXY_URL=https://api.meatnutrition.app
